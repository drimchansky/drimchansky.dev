---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'p'> {}

const { class: className, ...props } = Astro.props
---

<p class:list={['text-fg-muted font-sans text-base font-normal', className]} {...props}>
  <slot />
</p>
