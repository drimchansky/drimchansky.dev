---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'span'> {}

const { class: className, ...props } = Astro.props
---

<span class:list={['text-fg font-sans text-base font-normal', className]} {...props}>
  <slot />
</span>
