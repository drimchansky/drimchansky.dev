---
title: 'Тест контента'
description: 'Тестовая заметка, комбинирующая все возможные элементы контента для визуального регрессионного тестирования.'
publishingDate: 2020-01-01
lastModified: 2020-06-15
isTestOnly: true
isUntranslated: true
---

Параграф с **жирным текстом**, _курсивом_, **_жирный курсив_**, `инлайн код` и ~~зачёркнутый~~. Ссылка на [Пример](https://example.com) и эмодзи 🚀.

## Заголовок 2

Параграф под заголовком 2.

### Заголовок 3

Параграф под заголовком 3.

#### Заголовок 4

Параграф под заголовком 4.

## Списки

Маркированный список:

- Первый элемент
- Второй элемент с **жирным**
- Третий элемент с вложенными:
  - Вложенный элемент А
  - Вложенный элемент Б
    - Глубоко вложенный

Нумерованный список:

1. Первый элемент
2. Второй элемент с `инлайн кодом`
3. Третий элемент с вложенными:
   1. Вложенный элемент А
   2. Вложенный элемент Б

## Блоки кода

```typescript
interface User {
  name: string
  age: number
}

const greet = (user: User): string => {
  return `Привет, ${user.name}!`
}
```

```css
.container {
  display: flex;
  align-items: center;
  gap: 1rem;
  color: oklch(50% 0.2 240);
}
```

```sh
pnpm install
pnpm dev
```

## Цитаты

> Это цитата. Она может содержать **жирный** и _курсивный_ текст.

> Ещё одна цитата с длинным текстом для проверки переноса строк в отрендеренном выводе.

## Изображение

![Мем про вайб-кодинг](../../../components/notes/assets/vibe-coding-is-not-an-excuse_meme.jpg)

![Мем про вайб-кодинг](../../../components/notes/assets/vibe-coding-is-not-an-excuse_meme.jpg 'Мем про вайб-кодинг и технический долг')

---

## Смешанный контент

Параграф со [ссылкой](https://example.com), `кодом` и **выделением** в одной строке.

- Элемент списка со [ссылкой](https://example.com)
- Элемент списка с `кодом` и **жирным**

> Цитата перед блоком кода:

```typescript
console.log('Привет из контекста цитаты')
```

Финальный параграф для проверки нижнего отступа.
