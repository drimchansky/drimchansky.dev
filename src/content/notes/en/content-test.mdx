---
title: 'Content Test'
description: 'A test note combining all possible content elements for visual regression testing.'
publishingDate: 2020-01-01
lastModified: 2020-06-15
isTestOnly: true
---

A paragraph with **bold text**, _italic text_, **_bold italic_**, `inline code`, and ~~strikethrough~~. Also a link to [Example](https://example.com) and an emoji 🚀.

## Heading 2

Paragraph under heading 2.

### Heading 3

Paragraph under heading 3.

#### Heading 4

Paragraph under heading 4.

## Lists

Unordered list:

- First item
- Second item with **bold**
- Third item with nested:
  - Nested item A
  - Nested item B
    - Deeply nested

Ordered list:

1. First item
2. Second item with `inline code`
3. Third item with nested:
   1. Nested item A
   2. Nested item B

## Code Blocks

```typescript
interface User {
  name: string
  age: number
}

const greet = (user: User): string => {
  return `Hello, ${user.name}!`
}
```

```css
.container {
  display: flex;
  align-items: center;
  gap: 1rem;
  color: oklch(50% 0.2 240);
}
```

```sh
pnpm install
pnpm dev
```

## Blockquotes

> This is a blockquote. It can contain **bold** and _italic_ text.

> Another blockquote with a longer text to test wrapping behavior across multiple lines in the rendered output.

## Image

![A vibe coding meme](../../../components/notes/assets/vibe-coding-is-not-an-excuse_meme.jpg)

---

## Mixed Content

A paragraph with a [link](https://example.com), some `code`, and **emphasis** all in one line.

- List item with a [link](https://example.com)
- List item with `code` and **bold**

> A blockquote followed by a code block:

```typescript
console.log('Hello from blockquote context')
```

Final paragraph to test bottom spacing.
