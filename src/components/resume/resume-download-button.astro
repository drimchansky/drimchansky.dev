---
import { getLocale, t } from '@/i18n'
import { RESUME_FILENAMES_BY_LOCALE } from '@/utils/constants'

import ButtonLink from '../button-link.astro'
import TypographySpan from '../typography/TypographySpan.astro'

const locale = getLocale(Astro.currentLocale)
---

<ButtonLink
  class="print-button"
  download=`${RESUME_FILENAMES_BY_LOCALE[locale]}.pdf`
  href=`/files/${RESUME_FILENAMES_BY_LOCALE[locale]}.pdf`
>
  <TypographySpan>{t(Astro.currentLocale, 'download')}</TypographySpan>
</ButtonLink>

<style>
  .print-button {
    margin: 2.5rem auto 0 auto;
    background-color: var(--c-button-bg-origin);

    & span {
      color: var(--c-button-text);
    }
  }

  @media print {
    .print-button {
      display: none;
    }
  }
</style>
