---
import LogoIcon from '@/icons/logo-icon.astro'

const { class: className, ...rest } = Astro.props
---

<a class:list={['logo', className]} {...rest} href="/"><LogoIcon /></a>

<style>
  @import '../styles/mixins.css';

  .logo {
    @mixin focus;

    line-height: 0;
    z-index: 2;
    height: 3rem;
    width: 3rem;
    color: var(--c-text-primary);

    & svg {
      width: 100%;
      height: 100%;
    }
  }
</style>
